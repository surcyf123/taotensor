interface StatCardProps {
  icon: string;
  title?: string;
  value: string;
  maxValue?: string;
  description?: string;
}

export default function StatCard({
  icon,
  title,
  value,
  maxValue,
  description,
}) {}
